import React from "react";

import { bg_primary_color } from "../../styles";
import B from "./index";

const PrimaryButton = props => <B {...props} color={bg_primary_color} />;

export default PrimaryButton;
